spring:
  application:
    name: njnu-classroom-core
  cloud:
    nacos:
      server-addr: 10.144.0.252:8848
      config:
        namespace: njnu-classroom
        group: ${spring.profiles.active}
        file-extension: yaml
      discovery:
        namespace: njnu-classroom
        group: ${spring.profiles.active}
